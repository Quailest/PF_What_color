<div class="card-list-container py-5">
  <% @photos.each do |photo| %>
  <div class="card mx-auto mt-4 shadow-lg" style="width: 40rem;">
    <%= link_to public_photo_path(photo.id) do %>
      <%= image_tag photo.get_image, class:"card-img-top"; %>
    <% end %>
    <div class="card-body">
      <div class="row">
        <div class="col-2">
          <%= link_to public_user_path(photo.user) do %>
            <%= image_tag photo.user.get_profile_image(100,100),class:"mt-3 rounded-circle";  %></p>
          <% end %>
        </div>
        <div class="col-10">
          <p class="card-title">
            <%= link_to(public_photo_path(photo.id), class:"text-dark") do %>
              <strong>タイトル: <%= photo.title %></strong>
            <% end %>
          </p>
          <span>ニックネーム<%= photo.user.name %></span>
          <span>
            <%= link_to public_user_path(photo.user) do %>
            <% end %>
          </spam>
        </div>
      </div>
    </div>
  </div>
  <% end %>

</div>